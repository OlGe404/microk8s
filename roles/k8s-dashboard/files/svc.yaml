---
apiVersion: v1
kind: Service
metadata:
  name: k8s-dashboard
  labels:
    app: k8s-dashboard
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      nodePort: 30001
  selector:
    app: k8s-dashboard
