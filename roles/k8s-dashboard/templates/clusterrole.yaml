---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: k8s-dashboard
  labels:
    k8s-app: k8s-dashboard
{% if kubernetes_dashboard.skip_login is sameas false -%}
rules:
  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods", "nodes"]
    verbs: ["get", "list", "watch"]
{% endif -%}