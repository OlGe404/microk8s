---
kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: k8s-dashboard
  name: k8s-dashboard
  namespace: {{ kubernetes_dashboard.namespace }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      nodePort: 30001
  selector:
    k8s-app: k8s-dashboard