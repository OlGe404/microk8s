---
microk8s:
  add_ons:
    - dns
    - ingress
    - rbac
    - registry
    - fluentd
    - prometheus

kubernetes_dashboard:
  namespace: kube-system
  skip_login: true

grafana:
  username: admin
  password: admin